cron:
- description: store tweet into data store
  url: /gaedatastore
  schedule: every 2 hours
  # schedule: every monday 09:00
  # schedule: every 24 hours
# - description: pick hotwords from recent 100 tweets
#   url: /hotwords
#   schedule: every 60 hours
# - description: warm up the app by inserting 100 tweets in db
#   url: /warmup
#   schedule: every 24 hours

